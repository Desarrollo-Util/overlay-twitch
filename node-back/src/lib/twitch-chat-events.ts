import { Server as SocketServer } from "socket.io";
import { Client as TmiClient } from "tmi.js";

const startChat = (_tmiClient: TmiClient, _socketServer: SocketServer) => {};

export default startChat;
